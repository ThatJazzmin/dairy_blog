<%= form_for(@crafting) do |f| %>
  <% if @crafting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@crafting.errors.count, "error") %> prohibited this crafting from being saved:</h2>

      <ul>
      <% @crafting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: "form-control" %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit class: "btn btn-primary"%>
  </div>
<% end %>
